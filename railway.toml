# Root railway.toml — keep generic.
# Service-specific config lives in apps/<name>/railway.toml.
# Set each service's "Config Path" in the Railway dashboard accordingly.

[deploy]
restartPolicyType = "ON_FAILURE"
numReplicas = 1
